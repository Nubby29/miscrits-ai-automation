# Vision Fixtures

Saved screenshots from the dashboard are stored here for offline vision development.

Suggested categories:

- `exploration/`
- `battle/`
- `menu/`
- `inventory/`
- `loading/`
- `unknown/`

Fixtures are runtime artifacts and should only be committed when intentionally useful as test data.
